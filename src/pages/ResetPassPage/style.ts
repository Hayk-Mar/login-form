import { styled } from "styled-components";

export const StyledResetPass = styled.div`
    form {
        .input-wrapper {
            margin-bottom: 25px;
        }
        
        .btn {
            margin-bottom: 20px;
        }
    }
`;
