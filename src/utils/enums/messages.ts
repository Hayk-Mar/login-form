export enum Messages {
    TokenOrSecretIsNotExist = "Token or Secret is not exist in url, please check your link!",
}

export enum ValidationErrors {
    Length = "+ characters",
    Email = "Invalid email",
    Required = "Required"
}